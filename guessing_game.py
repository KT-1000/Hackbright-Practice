def how_many_guesses(num):
    """ Take a number, use binary search to see how many guesses it would take to get to that number
    """
    max_num = 101
    min_num = 0

    assert min_num < num < max_num, "Val must be between 1-100"

    num_guesses = 0

    while guess != num:
        if guess > num:


    return num_guesses